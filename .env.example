# the secret key would be generated by running make file.
SECRET_KEY=


# to generate your API Key you need to sign up on https://dashboard.stripe.com/register,
# then go to Dashbord and copy the secret key into the line.
# Additionaly you should copy the public key and replace the value with fresh generated
#in STRIPE_PUBLISHABLE_KEY in settings.py
stripe.api_key=